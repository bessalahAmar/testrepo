name: myotherworkflow
on: push
jobs:
    job1:
        strategy:
          matrix:
            contextlist: ["runner","job","github"]
        runs-on: ubuntu-22.04
        steps:
            - name: echo animal
              run: echo "${{toJson('$${{matrix.contextlist}}')}}"
