name: my first workflow
on: push
jobs:
  my_first_job:
    runs-on: ubuntu-latest
    steps: 
    - name: step one
      run: echo "step 1"
    - name: step 2
      run: ls
    - name: step 3
      run: cat README.md
